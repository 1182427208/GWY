# GwyPilot 项目面试问答

> 下面的回答按当前代码实现整理。面试时建议先讲结论，再根据面试官追问展开实现细节。

## 一、项目定位

### 1. GwyPilot 是做什么的？

GwyPilot 主要解决考公信息分散、备考规划缺乏个性化这两个痛点：把公告、政策、专业目录和岗位表统一接入，帮助考生完成政策查询、岗位筛选和备考规划。

核心是一个 Agent Loop，而不是固定的问答流水线。模型会根据用户目标自主判断下一步：先制定任务计划，按需加载岗位规划或文档处理 Skill，再通过 Tool Registry 调用岗位查询、动态 RAG、网页核验、风险复核和学习计划工具；拿到工具结果后继续判断是否需要补充检索，直到证据足够再生成答案。

底层岗位数据存 PostgreSQL，政策知识进入 Milvus；系统结合长短期记忆、上下文自动压缩、权限检查和 trace 记录，让 Agent 在长对话中保持用户目标，同时输出有依据、可追踪的报考建议。

### 2. 为什么这个场景适合 Agent，而不是普通 RAG？

普通 RAG 更适合“一个问题对应一次检索”。考公场景经常需要连续决策：先理解用户条件，再筛岗位，发现专业边界不确定后查目录，发现政策可能更新后做网页核验，最后再生成规划。

Agent 的价值在于根据中间结果动态决定下一步工具和检索范围。RAG 是 Agent 的知识工具之一，不负责替代岗位筛选、风险判断和任务编排。

### 3. 项目的整体链路是什么？

用户输入进入会话服务后，先加载画像、当前会话摘要、最近消息、附件和长期记忆。Agent Loop 决定是否写计划、加载 Skill 和调用工具；工具分别访问 PostgreSQL、Milvus、网页服务或业务 Agent。

工具结果写回共享 state，模型继续决策。最终输出 answer、citations、recommendations、risk review、study plan 和 retrieval trace，并按需要持久化为消息、任务、步骤和报告。

### 4. 你如何划分模型、规则和数据的职责？

模型负责意图理解、任务规划、查询改写、工具选择和自然语言表达；规则负责意图兜底、岗位硬条件和权限边界；数据库负责事实存储和精确查询；向量库和网页负责补充非结构化证据。

这个边界避免模型直接决定“是否符合报考资格”，也让每个结论都能追溯到数据、规则或检索证据。

## 二、Agent Loop、Skill 与工具调用

### 5. Agent Loop 具体是怎么运行的？

`AgentRuntime.run()` 初始化 system prompt、用户消息和上下文 state，然后最多循环 12 轮。每轮先执行 micro compact，超过 token 阈值时做 auto compact，再把工具 schema 交给模型。

如果模型返回 tool call，Runtime 进行权限检查、执行 handler、记录 `ToolUse/PostToolUse` 事件并把结果加入消息；如果没有 tool call，就停止并返回最终回答。达到轮数上限时根据 state 生成 fallback answer。

### 6. Agent 是如何决定调用哪些工具的？

不是代码里写死一条“检索—生成”流水线，而是通过 system prompt 给出任务约束，再由模型结合用户问题和已有 state 选择工具。例如政策问题调用 `search_policy_knowledge`，岗位问题调用 `search_positions_pg`，风险问题调用 `review_position_risks`。

工具结果会反馈给模型。如果证据不足，模型可以继续改写 query、扩大或调整检索范围；如果已经足够，则调用回答或报告工具结束流程。

### 7. Skill 解决什么问题？

Skill 是按需加载的任务级指令，不是另一个模型。Agent 通过 `load_skill` 从 runtime skills 目录加载岗位规划、文档处理等能力说明，将其写入当前 state，再按该 Skill 的约束调用工具。

这样通用 Agent 不需要把所有业务规则常驻在 prompt 中；Skill 负责领域流程和输出要求，Tool 负责真正执行，二者职责分离，便于测试和扩展。

### 8. Tool Registry 为什么重要？

Registry 把工具名称、参数 schema、handler 和上下文封装成统一接口，模型只看到结构化的工具描述，不直接接触 Python 函数实现。

这样可以集中做参数校验、权限检查、调用 trace 和错误恢复，也能把政策检索、岗位查询、网页读取等工具以 MCP 风格模块复用。

### 9. 如何避免 Agent 失控？

工具调用经过权限层：危险的 `bash`、写文件、编辑文件和删除记忆直接拒绝；搜索和审查工具属于只读白名单；持久化结果和飞书推送按业务策略处理；所有调用记录工具名、输入摘要、输出摘要、状态和耗时。

Agent 只拥有完成业务所需的最小能力，不能因为模型生成了一个工具名就获得任意系统权限。

### 10. 为什么需要上下文压缩？

Agent Loop 会不断累积模型消息、工具结果和证据，如果全部保留会增加 token 成本并降低模型决策质量。项目先做工具结果的微型压缩，超过阈值后调用摘要器生成连续性摘要，也支持模型主动调用 `compact` 指定保留重点。

压缩本身也会产生 trace，避免“上下文被截断但没人知道”的问题。

## 三、动态 RAG 与证据链

### 11. 动态 RAG 和普通 RAG 的区别是什么？

普通 RAG 通常是固定地把问题转 embedding、召回 top-k、拼 prompt。项目的动态 RAG 会根据意图、年份、考试类型和问题对象决定检索策略：改写多个 query、选择政策/指南/专业目录 collection，构建 metadata filter，并根据证据情况继续 follow-up 检索。

因此检索不是一次静态调用，而是 Agent 根据中间结果动态调整的证据获取过程。

### 12. 政策检索链路如何保证结果质量？

先做意图路由和 query 改写，再通过 Milvus 做向量召回，同时对候选计算 BM25 风格关键词分数；多个结果去重、融合后交给 Rerank 服务重排，最终由证据 Agent 判断是否足够支撑答案。

回答阶段只接收筛选后的 evidence，并输出 citations、rerank results 和 retrieval trace。没有可靠证据时返回不确定性，而不是让模型补写政策。

### 13. 为什么不能用 RAG 做岗位推荐？

向量相似度只能表示文本接近，不能严格判断学历、专业、政治面貌或基层年限是否满足。岗位推荐先从 PostgreSQL 读取结构化候选，再执行硬过滤和规则评分；RAG 只用于解释政策、专业目录和风险依据。

这是项目中最重要的数据边界：岗位资格是结构化决策，政策说明是证据检索。

### 14. 文档如何进入 RAG 知识库？

PDF 先经过文本、版面、图片和表格解析，识别问答、章节、表格行及跨页表，再生成带来源、页码、文档组、年份和考试类型的语义 chunk。Embedding 后写入 Milvus，同时在 PostgreSQL 保存文档和处理状态。

表格和图片不只转成一段文本，还通过 `GwyPdfAsset`、`GwyPdfTable` 和 `GwyPdfTableRow` 保存结构，方便引用和排错。

### 15. 检索不到证据时怎么处理？

证据 Agent 先观察命中数量、相关性和来源信息；不足时生成补充 query，再走检索和重排。如果仍不足，回答会明确说明当前知识库没有形成可靠依据，并提示查阅官方公告或人工确认。

系统不会把“模型有能力回答”当作“系统有证据回答”。

## 四、记忆与会话

### 16. 长期记忆和短期记忆如何分工？

短期记忆保存当前会话的最近消息、摘要、当前主题、附件和任务进度，用于维持局部上下文；长期记忆保存跨会话稳定的信息，例如用户画像、地区偏好、岗位偏好和历史决策。

会话和关键记忆落 PostgreSQL，Redis 作为缓存或加速层。Agent 通过 `load_memory`、`remember` 和 memory service 读取或更新，不把所有历史对话直接塞进 prompt。

### 17. 如何避免错误记忆污染后续回答？

记忆写入不是把模型每句话都永久保存，而是通过画像字段、偏好抽取和决策记忆等明确结构承载；对无效身份值、空值和不确定内容做过滤。长期记忆用于提供上下文，岗位资格仍以当前岗位数据和政策证据为准。

### 18. 会话如何支持回放？

消息表保存角色、意图、引用和 retrieval trace；Agent 运行保存 graph、step、tool call、输入输出和错误；分析任务还保存快照、步骤、报告和结果文件。

回放时可以区分是模型选择错误、工具返回错误、检索证据不足，还是报告生成问题，而不是只能看到最终文本。

## 五、岗位推荐、风险与规划

### 19. 岗位推荐的核心流程是什么？

先读取用户画像和筛选条件，从 PostgreSQL 获取岗位候选，再按学历、学位、专业、政治面貌、应届身份、基层经历和地区做硬过滤，之后进行匹配评分和风险分级。

结果持久化为 recommendation task 和 recommendation item，保存排名、分数、匹配理由、风险、引用及是否需要人工确认。

### 20. 为什么岗位分析要求固定岗位快照？

快照把一次分析的候选集合冻结下来，保证后续 Agent 在同一批岗位上工作。否则岗位表或筛选条件变化后，报告无法复现，也无法解释排名变化。

因此直接在聊天中要求岗位推荐但没有 snapshot 时，系统会先引导用户在岗位表中筛选并固定快照。

### 21. 风险复核 Agent 做什么？

它不重新替代岗位硬筛选，而是针对专业目录边界、基层年限、户籍、证书、政治面貌等不确定条件生成证据查询，并对证据进行检索和重排。

最终输出风险类型、等级、依据、解释、建议和人工确认标记，解决“匹配度高但报考仍可能有风险”的问题。

### 22. 学习计划如何和岗位分析连接？

学习计划 Agent 读取考试年份、考试类型、用户每日学习时长、画像和岗位分析结果，生成科目权重、阶段目标、周任务和每日任务，并分别持久化计划、阶段、科目和任务。

这样规划不是脱离岗位的通用日历，而是把用户目标、考试要求和岗位选择放在同一个任务上下文中。

### 23. 报告生成为什么单独拆出来？

岗位决策负责算候选和风险，报告生成负责将候选、证据、风险和下一步行动组织成稳定 Markdown。拆开后可以复用同一份结构化结果到前端、归档和飞书推送，也能独立测试报告格式和内容清理。

## 六、网页、附件与工程可靠性

### 24. 为什么还需要网页核验？

政策和岗位信息存在年度变化，本地知识库不一定实时。网页 Agent 会根据岗位和风险目标生成查询，搜索后抓取正文；HTTP 读取失败或页面动态渲染时，再使用 Playwright fallback。

网页是时效性补充证据，不绕过本地知识库，也不替代 PostgreSQL 的结构化岗位过滤。

### 25. PDF 和图片如何进入 Agent 上下文？

附件先落盘并记录处理状态，PDF 优先抽文本，扫描件或文本不足时按页转图片；图片和页面资产可经过多模态模型得到摘要或 OCR 文本，最后统一形成可检索的附件上下文。

原始文件、抽取文本、摘要和元数据分别保存，后续工具可以知道证据来自哪个附件、哪个页面。

### 26. 外部服务失败时如何降级？

Redis 失败回退 PostgreSQL；搜索或抓取失败切换备用搜索或 Playwright；Agent Loop 出错进入确定性 fallback；RAG 证据不足则补充检索或明确返回人工确认。

降级结果仍写入状态和 trace，保证用户知道系统是完成了判断、部分完成，还是没有获得足够证据。

### 27. 如何评估这个系统，而不只是看“能不能回答”？

需要分层评估：岗位硬过滤准确率、RAG 召回率、rerank 相关性、引用正确率、工具选择准确率、Agent 完成率、上下文压缩后的任务连续性、端到端延迟和 token 成本。

尤其要建立带官方答案和引用的问题集，分别测试政策咨询、专业匹配、岗位推荐和风险复核，避免只用主观体验评价 Agent。

### 28. 这个项目最难的地方是什么？

最难的不是接入模型或实现一个检索接口，而是把 Agent 的自主性约束在公务员报考的业务边界内。它可以动态选择 Skill、工具和检索路径，但岗位资格必须回到 PostgreSQL 的结构化规则，政策结论必须有 RAG 证据，同时还要保留记忆、状态和 trace，做到过程可控、结果可解释、问题可回放。

### 29. 项目目前最大的不足是什么？

一是知识库还需要更完整的官方数据更新、版本管理和失效机制；二是岗位规则和评分权重需要真实反馈校准；三是 Agent、RAG 和工具调用还应建立持续评测与观测体系。

此外，任何涉及最终报名资格、资格审查和政策时效的结论，都必须保留官方公告和人工核验边界。

### 30. 如果继续优化，你会优先做什么？

我会先做数据和评测闭环，而不是继续增加 Agent 数量：用失败样本分析是岗位字段、检索、工具选择还是回答引用出了问题，再分别调整数据清洗、规则、Skill、检索策略和 prompt。

在此基础上再优化异步任务、缓存、模型成本和知识库版本切换，确保系统规模扩大后仍然可解释、可回放。

### 31. 用一句话总结项目价值。

GwyPilot 把分散的考公信息和零散的备考决策，组织成一个由 Agent Loop 驱动、动态检索和工具调用支撑、具备记忆与追踪能力的报考和备考辅助系统。
