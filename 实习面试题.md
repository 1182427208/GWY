# GwyPilot 面试回答稿

这份稿子只讲真实实现，不讲空泛功能。你面试时可以先用前面的“项目总览”完整介绍项目，再往后回答细节问题。

## 项目总览

如果面试官让你完整介绍项目，建议先这样讲：

> GwyPilot 是一个面向公务员岗位分析和备考辅助的智能平台，基于 `fastapi/full-stack-fastapi-template` 二次开发，后端保持模板原有骨架，只在 `backend/app/gwy/` 下扩展业务能力。  
> 这个项目不是一个普通聊天机器人，而是把“岗位结构化筛选、政策知识检索、网页核验、多轮会话记忆、风险复核、报告生成、学习计划和飞书推送”串成了一条可追踪的 Agent 流程。  
> 在数据层面，岗位库进 PostgreSQL，政策、报考指南和专业目录进 Milvus；在会话层面，短期上下文和缓存用 Redis，长期画像和历史记录落 PostgreSQL；在推理层面，用 LangGraph 编排多个 Agent，把检索、判断、核验和生成拆成明确节点。  
> 我自己在项目里主要做的是把这些能力工程化落地，包括岗位导入、会话和记忆层、政策 RAG、多模态附件处理、网页证据补充、分析任务编排和 trace 记录，让整个系统不是黑盒，而是能回放、能解释、能扩展。

如果面试官追问“这个项目到底在解决什么问题”，可以继续补一句：

> 公务员岗位和政策信息分散、更新快，而且很多判断必须依赖硬条件，不能靠大模型自由发挥。所以这个项目的核心思路是：先用结构化数据库把岗位条件筛准，再用向量检索和网页核验去补政策证据，最后通过多 Agent 把推荐、风险和报告组织成一个可审计的结果。

如果你想要的是像图里那种“完整流程段落版”，可以直接这样讲：

> 这个项目的完整流程是这样的：用户先在前端进入岗位分析或政策问答场景，岗位数据和用户画像先由后端统一接收和保存。岗位库这一部分走 PostgreSQL，负责学历、专业、地区、政治面貌、基层年限这类硬条件筛选，不让大模型直接决定岗位是否可报。政策、报考指南、专业目录这类文档会先进入文档解析链路，系统会把 PDF、图片和表格拆成可检索的结构化块；如果是扫描件或截图，会先做 OCR 和多模态摘要，把图像内容转成文本证据，再把这些内容按 chunk 写入 Milvus，供后续语义检索。  
> 用户在聊天里提出问题后，后端先用 Redis 和会话表恢复当前上下文，包括最近消息、会话摘要、附件、短期偏好和长期画像，再交给 LangGraph 的状态机路由到不同节点。如果问题是岗位推荐，就先走 PostgreSQL 做硬筛，再用政策证据和网页核验补充风险判断；如果是政策问答，就先做问题改写，再走 Milvus 检索、融合、重排和证据拼接，最后由答案节点生成可引用的回复。整个过程中，系统会把每一步的输入、输出和证据都写成 trace，必要时还能把分析结果生成报告并推送到飞书。  
> 所以这个项目不是简单的“问答 + 检索”，而是把结构化筛选、文档解析、向量召回、网页核验、会话记忆和多 Agent 编排组合成了一条完整的技术链路。我的工作重点就是把这条链路做稳，让系统既能答得出来，也能说清楚为什么这么答。

## 一、项目整体技术架构

1. **面试官：先用一句话介绍一下这个项目的技术架构。**

   答：这个项目的底层是 PostgreSQL + Redis + Milvus 三层数据架构，上层用 LangGraph 把多个 Agent 串成一条可追踪的状态机。结构化数据放 PostgreSQL，短期上下文和缓存放 Redis，长期画像和审计记录放 PostgreSQL，政策和指南这类非结构化知识进 Milvus，网页证据通过 Fetch 和 Playwright 补充。

2. **面试官：为什么你们要分这么多层，而不是全放一个库里？**

   答：因为访问模式不同。岗位筛选需要精确条件过滤，所以必须用关系型数据库；会话上下文需要低延迟、高频读写，所以适合 Redis；长期记忆需要持久化和可审计，所以放 PostgreSQL；政策和指南需要语义召回，所以放 Milvus。分层之后，查询性能、缓存策略和数据一致性都更清楚。

3. **面试官：这个项目里的“可追踪”具体指什么？**

   答：每一次分析、检索、风险复核和报告生成，都会被拆成节点级步骤并记录 trace。trace 里会存输入摘要、输出摘要、证据引用和执行状态。最后不仅能看到结果，还能回看“为什么得出这个结果”，这对调试和复盘很重要。

## 二、长短期记忆怎么实现

4. **面试官：你们的短期记忆具体怎么实现？**

   答：短期记忆不是把整段历史直接塞进 prompt，而是做成工作记忆块。实现上，我用 Redis 做 TTL 缓存，key 里包含 `user_id`、`conversation_id` 和 `memory_key`，value 是 JSON；默认 TTL 是 86400 秒。写 Redis 的同时，会把同样的内容同步写到 PostgreSQL 的 `gwy_conversation_memory` 表里，Redis 挂了就从数据库兜底读取。

5. **面试官：短期记忆到底缓存了哪些内容？**

   答：缓存的是当前轮推理真正需要的状态，而不是聊天全文。比如 `analysis_progress`、`task_context`、`last_recommendations`、`user_preferences`、会话摘要、当前话题和最近消息。这些信息足够支撑下一轮推理，但不会把上下文撑得太大。

6. **面试官：长短期记忆的区别是什么？**

   答：短期记忆是“当前会话的工作状态”，目标是低延迟、可过期；长期记忆是“跨会话的稳定偏好和历史决策”，目标是可持久、可更新、可审计。短期记忆放 Redis + 会话表，长期记忆放用户画像表、决策表和经验表，再用 Redis 缓存长期摘要。

7. **面试官：长期记忆具体怎么实现？**

   答：长期记忆不是让模型“自己记住”，而是显式落库。系统会把用户画像、岗位决策、历史分析记录和经验模式分别写到 `gwy_user_profile`、`gwy_decision_memory`、`gwy_recommendation_task`、`gwy_experience_memory` 这些表里。然后再基于这些表生成一个跨会话摘要，并用 Redis 缓存摘要结果，TTL 也是 24 小时。

8. **面试官：长期记忆是怎么更新的？**

   答：更新分两部分。第一部分是从用户消息里抽取偏好，靠规则模式去抓地区、专业、学历、昵称这类显式信息；第二部分是从历史行为里累计，比如用户更常看哪些岗位、点过哪些推荐、做过哪些分析。抽出来之后，会做去噪、去重和归一化，不会直接把所有文本都写进去。

9. **面试官：你们为什么要给记忆做缓存？**

   答：因为长期记忆和会话摘要会被频繁读取，如果每次都扫数据库，成本会很高。所以做了 Redis 缓存层，常用的跨会话摘要和短期上下文都先读缓存，缓存失效或不可用时再回退到数据库。

10. **面试官：Redis 挂了会怎样？**

    答：不会影响核心功能，因为 Redis 只是加速层。短期记忆会回落到 PostgreSQL 的 `gwy_conversation_memory`，长期摘要会重新从数据库聚合。最多是响应慢一点，不会直接把会话打断。

11. **面试官：你怎么避免记忆越记越乱？**

    答：我做了三层过滤。第一，抽取偏好时只认明确表达；第二，写入长期画像前会过滤明显无效字段，比如疑问词、占位词、过长字符串；第三，长期记忆 prompt 里会明确标注“仅供参考，遇到冲突以用户最新说法为准”。这样能避免脏数据污染后续推理。

## 三、会话记录怎么实现

12. **面试官：会话记录是怎么存的？**

    答：会话是独立建模的，核心表是 `gwy_chat_session`、`gwy_chat_message` 和 `gwy_chat_attachment`。一个 session 对应一个会话上下文，消息表存每轮 user/assistant 消息，附件表存用户上传的文件、类型、摘要、提取文本和处理状态。这样会话、消息和附件是分开的，查询和清理都更方便。

13. **面试官：每轮对话是怎么把上下文接起来的？**

    答：请求进来后，后端会先按 `session_id` 和 `user_id` 找到会话，再读取最近消息、会话摘要、当前话题、历史附件和长期画像。然后把这些内容拼成 memory context，交给 RAG 或分析 Agent。上下文不是前端拼的，而是后端统一组织的。

14. **面试官：为什么要把消息、摘要和附件分开存？**

    答：因为它们的生命周期不同。消息是完整日志，适合留痕；摘要是压缩态，适合推理；附件是外部证据，适合后续引用。分开存能让检索更高效，也能让清理、回放和重建上下文更容易。

15. **面试官：附件“已消费”是什么意思？**

    答：附件第一次被注入到当前会话上下文之后，会在 metadata 里打上 `context_consumed` 标记。下次再构建上下文时，只取未消费的附件，避免同一份文件反复注入导致上下文重复和膨胀。这是一个很实用的控制策略。

16. **面试官：删除会话时怎么处理？**

    答：删除会话不是只删主表，而是会连带删掉消息、附件、conversation memory、RAG cache entries，并且把 Redis 缓存 key 和附件文件一起清掉。这样不会留下孤儿数据，也不会出现删了会话但缓存还在的问题。

## 四、图片、PDF、语音怎么实现

17. **面试官：图片上传后，后端是怎么处理的？**

    答：图片先落盘，然后调用多模态摘要服务。这个服务会把图片读成 base64，再连同附近文本一起发给支持多模态的模型，让模型返回摘要和 OCR 风格的文本结果。返回后保存成附件记录，里面有 `summary`、`ocr_text`、`extraction_status` 和图片路径。

18. **面试官：图片识别为什么要带附近文本？**

    答：因为单独看图有时信息不完整，尤其是表格截图、页面截断图或者扫描图。附近文本能给模型补上下文，比如知道这张图来自第几页、图前后在讲什么，这样摘要会更稳定。

19. **面试官：如果多模态识别失败怎么办？**

    答：会走降级逻辑。服务会返回一个 `pending_multimodal_summary` 的状态，同时生成基于文件名、页码和附近文本的 fallback summary。这样即使模型失败，系统也不会完全拿不到证据，而是保留一个可用的最小摘要。

20. **面试官：PDF 是怎么处理的？**

    答：PDF 先尝试直接抽文本。如果抽得到文本，就直接做摘要；如果抽不到，就把 PDF 按页转成图片，再走多模态摘要。也就是说，处理链路是“文本优先，图片兜底”。这比只靠一个 OCR 稳定很多。

21. **面试官：PDF 里的图像和表格怎么进系统？**

    答：页面转图后，会把每页的图像摘要和 OCR 文本记录下来，再把这些内容作为附件上下文或者文档 chunk 的补充信息。对于表格，系统会保留页码、bbox、markdown 内容和行级信息，方便后续检索和解释。

22. **面试官：语音是怎么处理的？**

    答：当前实现的是 TTS，也就是文本转语音。后端会先把文本做规范化，然后调用第三方模型的 speech 接口，返回音频字节给前端。语音输入如果有的话，本质上也是先转成文本，再走同样的对话和检索流程，所以后端主链路始终是文本语义。

23. **面试官：为什么最后都要统一成文本？**

    答：因为后面的核心能力都在文本层完成，包括记忆、检索、重排、证据拼接和生成。把图片、PDF、附件和语音统一转成文本上下文，可以大幅简化系统复杂度，也更容易做 trace 和回放。

## 五、多 Agent 是怎么实现的

24. **面试官：你们的 multi-agent 是怎么拆分的？**

    答：不是一个大 Agent 包办所有步骤，而是按职责拆成多个节点式 Agent。比如岗位决策 Agent 负责岗位筛选和排序，风险复核 Agent 负责识别报考风险，报告生成 Agent 负责组织结构化输出，网页核验 Agent 负责补充外部证据，飞书推送 Agent 负责发送结果。每个 Agent 都是独立状态机，输入输出清晰。

25. **面试官：这些 Agent 怎么协作？**

    答：协作靠 LangGraph 的状态图。所有节点共享同一份 state，每个节点读自己需要的字段，处理完后把结果写回 state，再流向下一个节点。状态图里明确写了节点顺序、条件分支和终止条件，不是松散的函数互调。

26. **面试官：LangGraph 在这里解决了什么问题？**

    答：它解决的是“多步骤任务编排”和“状态可追踪”两个问题。这个项目里有意图路由、问题改写、检索、融合、风险复核、报告生成、推送这些步骤，如果不用状态图，很容易写成一堆耦合函数。用 LangGraph 之后，每一步的入口、出口和分支都能显式表达。

27. **面试官：为什么不用一个 Agent 一把做完？**

    答：因为一个 Agent 同时做检索、判断、复核、写报告，很容易把问题搞成黑盒，出了错也不好定位。拆成多个 Agent 后，检索只负责找证据，风险只负责判风险，报告只负责组织表达，职责更单纯，也更容易写测试。

28. **面试官：多 Agent 之间怎么传递结果？**

    答：通过共享 state 传递。state 里会包含 query、user profile、retrieval results、evidence block、risk items、recommendations、trace、attachments、memory context 等字段。每个节点只更新自己的部分，后面节点继续读这个 state，所以整个流程是显式的数据流。

29. **面试官：怎么保证多 Agent 的输出可追踪？**

    答：每个节点都会往 trace 里写结构化记录，包括 step 名称、输入摘要、输出摘要、证据引用和状态。最后这些 trace 会落到 task 表和 step 表里，所以你能从最终结果一路追到中间每个步骤。

30. **面试官：岗位决策 Agent 的技术逻辑是什么？**

    答：它先基于用户画像和查询条件抽取过滤规则，再从岗位表里做结构化筛选和打分。筛选后会给出推荐等级、风险等级和理由，同时把命中的岗位、未命中的原因和得分写回状态。它本质上是一个“数据库筛选 + 规则打分 + 结果解释”的 Agent。

31. **面试官：风险复核 Agent 是怎么做的？**

    答：它会先从岗位或分析上下文里提炼出风险假设，比如基层年限、专业测试、户籍、证书要求等，再把这些假设转成证据查询，去政策知识库和外部证据里检索。拿到证据后，再按照证据强度把风险分成高、中、低，并输出是否需要人工确认。

32. **面试官：报告生成 Agent 具体怎么组织输出？**

    答：它不是直接一段自然语言糊出来，而是先规划 outline，再填充分析结论、证据、风险和建议，最后做一次 clean-up。这样输出结构比较稳定，前端展示和飞书推送也更容易。

## 六、RAG 和网页核验怎么实现

33. **面试官：政策问答的检索链路具体怎么跑？**

    答：先做意图识别，判断是直答、政策 RAG 还是岗位推荐；如果需要 RAG，就先改写查询，再去 Milvus 做召回；召回结果会去重、融合、重排；如果证据不够，还会再做一次 follow-up 检索；最后把证据块拼进 prompt 里生成答案。

34. **面试官：为什么要做问题改写？**

    答：因为用户的原始问题通常太口语化，不利于召回。改写之后会补上更适合检索的关键词，比如年份、考试类型、岗位限制和专业名，这样召回效果更稳定。

35. **面试官：为什么检索结果还要 rerank？**

    答：向量召回擅长找“相关”，不一定最适合直接回答。rerank 负责把真正能支撑答案的证据往前排，减少噪声。这个项目里会先融合多个查询的结果，再做 rerank，最终只保留最核心的证据块。

36. **面试官：网页搜索和抓取怎么实现？**

    答：搜索层优先走 SearXNG，如果没配好或者不可用，就可以回退到其他搜索源。抓取层优先用 Fetch，拿不到或者页面动态渲染时，再用 Playwright。Playwright 支持本地浏览器读取，也支持通过 MCP server 调用远程 browser 工具。

37. **面试官：为什么要支持 MCP 方式的网页读取？**

    答：因为有些环境里浏览器能力是外置服务，直接本地跑 Playwright 不一定稳定。MCP 方式可以把网页读取能力封装成标准工具接口，Agent 侧只管调用，底层是本地浏览器还是远程浏览器，不影响上层逻辑。

38. **面试官：网页正文怎么转成可用证据？**

    答：抓取到页面后，会先抽取 title、正文和最终 URL，再把干净文本归一化。后续进入证据块时，会保留来源、标题、正文和抓取方式，方便回答里引用。

## 七、会话、缓存和数据落库

39. **面试官：聊天记录是怎么存的？**

    答：消息会单独存成 `gwy_chat_message`，会话元数据存 `gwy_chat_session`，附件存 `gwy_chat_attachment`，会话级缓存存 `gwy_rag_cache_entry`。这样设计的好处是消息日志、摘要状态、附件证据和查询缓存各自独立，维护起来更清楚。

40. **面试官：为什么要把 session summary 单独存？**

    答：因为完整消息太长，下一轮推理不可能每次都读全量历史。summary 相当于压缩过的工作上下文，既保留主线信息，又不会让 prompt 失控。系统会在每轮结束后更新会话摘要，再把摘要写回数据库和记忆层。

41. **面试官：RAG 查询缓存怎么做？**

    答：查询结果会先算一个 cache key，再优先读 Redis。如果 Redis 命中，就直接返回缓存结果；如果没命中，就去 Milvus 检索，然后把结果写回 Redis 和 PostgreSQL。这样高频重复问题可以直接复用检索结果。

42. **面试官：为什么还要把缓存写进 PostgreSQL？**

    答：因为 Redis 是快缓存，不适合做唯一数据源。把缓存记录同步存进数据库，一方面可以兜底，另一方面也方便审计和回放。即使 Redis 清空了，也能从数据库恢复结果。

43. **面试官：用户画像是怎么更新的？**

    答：用户画像既可以通过显式表单更新，也可以通过对话中的偏好抽取自动更新。比如用户说“我想去浙江”“我是应届”，系统会把这些字段合并到 `gwy_user_profile`，再用长期记忆服务做统一整理和缓存失效。

## 八、项目里最容易被追问的点

44. **面试官：这个项目里你最想强调的技术点是什么？**

    答：我最想强调的是“分层记忆 + 状态机编排 + 证据化输出”。短期记忆用 Redis TTL 处理，长期记忆用 PostgreSQL 沉淀，复杂流程用 LangGraph 管理状态，最后每个输出都带证据和 trace。这样系统不是一个黑盒聊天器，而是一个可控的工程系统。

45. **面试官：你为什么说这个项目是你自己做的？**

    答：因为很多关键实现不是简单调接口，而是需要自己设计数据流和状态流。比如 Redis key 的组织、会话上下文的压缩、附件消费机制、LangGraph 节点拆分、证据拼接、缓存失效和降级路径，这些都要根据项目场景自己落地，不是模板里直接给的。

46. **面试官：如果让你重新讲一遍工作内容，你会怎么说？**

    答：我主要做的是把大模型相关能力工程化。具体就是把结构化筛选放到 PostgreSQL，把短期会话上下文放到 Redis，把长期画像和历史决策落到 PostgreSQL，再通过 LangGraph 把检索、核验、风险复核和报告生成串成一个可追踪的状态流。图片、PDF、语音和网页内容都被统一转成可检索的文本证据，方便后续推理。
