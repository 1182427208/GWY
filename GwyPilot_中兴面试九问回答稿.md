# GwyPilot 中兴面试九问回答稿

> 以下答案按当前项目实现整理，适合面试时直接口述。遇到追问，再补充“项目实际”。

## 1. 你的 Agent Memory 是怎么设计的？

### 建议回答

我的 Memory 主要分成短期记忆和长期记忆两部分。

短期记忆就是当前会话的工作状态，保存用户当前目标、任务进度、最近确认的信息、工具调用结果和上下文摘要。它主要服务当前任务，不会把完整聊天记录一直放进 Prompt。工具结果太长时，我会先保存原始 transcript，只把摘要放进上下文，需要时再根据 ID 查原始内容。

长期记忆主要是用户画像和历史行为。用户画像包括学历、专业、政治面貌、是否应届、意向地区和岗位偏好；历史行为包括看过、喜欢或不喜欢的岗位，以及过去的岗位分析记录。这些数据主要存 PostgreSQL，常用的摘要用 Redis 加速。

读取时不是把所有记忆都加载出来，而是根据当前任务按需读取。更新时，当前版本主要通过字段抽取、规则和关键词匹配完成；新信息一般覆盖或补充旧信息，并过滤明显无效的内容。

### 项目实际

当前项目已经实现了短期工作记忆、长期用户画像、岗位决策记录、跨会话摘要、Redis 加速和 PostgreSQL 兜底。但还没有完整实现带来源和置信度的记忆体系，也没有做纯语义的长期事件记忆检索，所以 `LLM side-query` 不能说得过满。

## 2. 短期记忆除了截断和摘要，还有什么更好的方案？

### 建议回答

我认为更好的方式是把短期记忆做成结构化工作状态，而不是简单保留最近几轮对话。

这个状态可以保存任务目标、硬约束、已完成步骤、未完成步骤和已经确认的事实。工具结果则放在外部，只在上下文里保留摘要和引用，需要时再读取原始结果。

另外可以按重要性保留信息。用户的硬条件、已经验证的事实和关键决策优先保留，重复内容和低价值中间结果可以压缩或删除。历史对话也可以按主题分段，根据当前问题检索相关片段。

### 项目实际

当前项目已经支持微压缩、Token 超限自动压缩和模型主动调用 `compact`。压缩触发不应该只说成“三次工具调用”，更合理的是根据 Token 预算、工具结果长度和任务阶段判断。

## 3. 为什么要使用多智能体？协作机制是什么？

### 建议回答

这个项目采用的是主 Agent 调度多个子 Agent 的方式。主 Agent 负责理解用户输入、拆解任务和选择下一步要调用哪个子 Agent，子 Agent 只负责完成具体任务。

具体流程是，用户输入进来后，主 Agent 会先根据问题生成一个 `todo_write` 计划，把任务拆成几步。然后主 Agent 根据这个计划决定调用哪个子 Agent，比如岗位筛选、政策检索、风险复核、报告生成或者学习计划。

子 Agent 不负责整体规划，也不会直接生成最终答案。它只是完成自己的那一部分任务，然后把结构化结果返回给主 Agent。最后由主 Agent 统一汇总这些结果，生成最终回答。

另外，子 Agent 不能再创建新的子 Agent，这是为了防止无限递归和调用失控。子 Agent 执行过程中的完整上下文也不会全部传回主 Agent，只会返回必要的结果摘要、结构化数据和 trace。这样可以避免子任务里的大量工具输出污染主 Agent 的上下文。

所以整体上，它不是多个 Agent 随便互相对话，而是主 Agent 做调度，子 Agent 做具体执行，结果再回到主 Agent 汇总。

### 项目实际

当前项目更准确地说是“一个主 AgentRuntime 加多个专项子 Agent/服务”。它借鉴了 subagent 的上下文隔离思想，但没有做递归子 Agent，也不是所有子 Agent 都并行运行。这样实现更简单、可控，也方便 trace 追踪。

## 4. 你怎么评估系统回答是否正确、质量是否优秀？

### 建议回答

我会分层评估，而不是只看最终回答是否通顺。

第一层看检索，主要看正确的文档或切片有没有被召回，引用能不能支撑回答。第二层看任务结果，岗位筛选会用确定性规则检查学历、专业、政治面貌等硬条件，不能把不符合条件的岗位推荐出来；政策问答则检查关键结论是否有证据。

第三层看 Agent 过程，比如工具是否选对、参数是否正确、有没有无效调用、任务失败后能不能恢复。最后再看延迟、调用步数、Token、费用和错误率等系统指标。

另外还要做对比实验，比如单 Agent 和多 Agent、有无 Memory，确认每个模块确实带来了收益。

### 项目实际

当前项目已经有单元测试、集成测试、岗位规则测试、引用结果和 trace，评测设计也已经明确了岗位约束、RAG、Memory 和效率指标。完整的离线数据集、自动化评测报告和统一看板还在补充，所以目前不能虚构具体提升百分比；Token 或费用没有采集到时，也会标记为不可用。

## 5. 怎么保证信息的时效性和正确性？

### 建议回答

我会从数据来源、检索方式和结果验证三个方面保证信息的时效性和正确性。

首先，岗位表和国考政策相关的 PDF 都从官方网站下载，保存来源、年份和考试类型等信息。岗位数据解析后进入 PostgreSQL；政策、报考指南等文档经过切分和向量化后进入 Milvus，同时保留原始文档和来源信息。这样岗位筛选和政策问答都有明确的数据出处。

其次，岗位筛选使用 PostgreSQL 的结构化条件和确定性规则匹配学历、专业、政治面貌、地区等要求，不用语义检索替代岗位过滤，确保推荐结果来自真实岗位数据。政策问答使用 RAG 检索对应文档；对于招录比、当前公告等可能变化的信息，则通过网页检索工具到官方网站核验，并标注数据时间。

最后，生成回答时要检查岗位硬条件是否满足，政策结论是否有文档或网页证据支持。RAG 使用到的文档和切片会在前端会话底部展示，用户可以直接查看检索来源。如果材料不足或来源之间存在差异，就明确提示需要进一步核验，不让模型自行补全。

### 项目实际

当前项目已经实现了官方岗位数据入 PostgreSQL、政策文档向量化入 Milvus、结构化岗位筛选、网页补证、引用记录和前端来源展示。完整的数据集版本管理、知识更新后的自动失效和统一的过期信息评测还需要继续补齐。

## 6. 你有没有看 API 缓存命中率？

### 建议回答

我会先区分三类缓存：模型供应商的 Prompt Cache、应用层回答或检索结果缓存，以及 Embedding 和检索缓存。

当前项目主要做了应用层缓存，包括政策检索结果、岗位列表和长期记忆摘要。Redis 用来加速，PostgreSQL 用来兜底和审计。缓存指标应该包括命中率、P95 延迟、节省的 Token 和费用，以及过期缓存比例。

缓存 Key 不能只用用户问题，还要考虑年份、考试类型、文档版本、模型版本和用户权限。知识库更新后，也要让相关缓存失效。

### 项目实际

当前缓存已经接入，但还没有完整记录 hit、miss、节省 Token 和节省费用，缓存失效和 TTL 监控也不完整。这个问题我会坦诚说“已经有缓存，但监控体系还需要补齐”。

## 7. ViT 输出怎么与 Text Embedding 空间对齐？

### 建议回答

ViT 会把图片切成 Patch，经过线性映射和位置编码后得到视觉 Token。因为视觉特征维度和语言模型维度通常不同，所以需要一个 Projector、Q-Former 或 Resampler，把视觉特征映射到语言模型的 Token 空间。

映射后的视觉 Token 可以和文本 Token 拼接，也可以通过 Cross-Attention 融合。CLIP 更偏向把图像和文本映射到共享空间，而生成式多模态模型还要把视觉 Token 接入语言模型进行生成。

### 项目实际

我的项目没有训练 ViT 或 Projector，采用的是应用层方案：用多模态模型生成图片摘要和 OCR 文本，把摘要用于检索，同时保留原图和附件信息用于追溯。

## 8. MoE 的基本结构是什么？

### 建议回答

MoE 通常替换 Transformer 里的 FFN 层。每个 Token 先经过 Router，Router 选择 Top-K 个专家处理，再把这些专家的结果加权合并。

这样模型可以拥有很多参数，但每个 Token 只激活少数专家，所以单次计算量不会按总参数量完全增长。

它的难点主要是专家负载不均衡、Token 容量限制和分布式通信，通常需要负载均衡损失、容量控制和通信优化。优点是模型容量大，缺点是训练和部署更复杂。

### 项目实际

GwyPilot 当前调用的是外部大模型 API，没有实现 MoE 的训练或专家路由。这道题按模型原理回答，不要说成自己做过 MoE。

## 9. 量化误差指标怎样和下游任务分数做关联分析？

### 建议回答

我会把每种量化方案作为一个样本，把 MSE、L2 Norm、余弦距离、最大误差和误差方差作为特征，把相对 FP16 模型的下游分数下降作为目标。

先用 Pearson、Spearman 或 Kendall 看相关性，再用 Lasso、随机森林或梯度提升树做多变量分析。同时控制模型规模、量化位宽、层类型和任务类型这些因素。

最后要在没有参与分析的新模型、新位宽和新任务上验证。如果一个指标既能预测分数下降，又能正确排序不同量化方案，才适合作为量化质量的代理指标。

### 项目实际

当前 GwyPilot 没有做量化实验，这一题属于通用的模型评测方法，不能说成项目已经产出的实验结果。

## 10. 这个项目里的 LLM side-query 是怎么实现的？

### 建议回答

这个项目里的 side-query 主要解决的是记忆按需加载问题。因为长期记忆会不断增加，如果每次都把全部记忆放进主 Agent，不仅会增加上下文长度，也容易引入与当前任务无关的信息。所以我的做法是，先让一个独立的小模型判断这次任务到底需要哪些记忆。

具体流程是，系统先把已有记忆整理成一组轻量级记忆卡片，比如用户画像、当前会话偏好、最近岗位选择和历史岗位分析等。主 Agent 不会直接读取全部记忆正文，而是把当前问题、最近对话摘要和记忆卡片目录交给 side-query。

side-query 只负责选择相关记忆，不负责回答用户问题。它会返回需要加载的记忆名称，系统再根据这些名称读取对应内容，并进行合法性校验、去重和长度限制，最后把选中的记忆注入主 Agent 的上下文。这样主 Agent 看到的是与当前任务相关的少量记忆，而不是全部历史记录。

存储方面，长期记忆主要保存在 PostgreSQL 中。用户画像、岗位决策和跨会话偏好等数据都有对应的持久化记录，不是以文件形式保存。Redis 主要用于缓存长期摘要和短期工作记忆，作用是加速读取；即使 Redis 没有命中或暂时不可用，系统也可以从 PostgreSQL 重新加载，所以 PostgreSQL 才是长期记忆的主要数据来源。

如果 side-query 超时、返回格式不正确，或者模型调用失败，系统就不注入记忆，也不会回退到全量记忆读取。这样可以避免无关记忆进入主 Agent，同时保证记忆选择模块异常时不影响主流程。

记忆写入方面，目前项目主要采用规则抽取，而不是使用 LLM 抽取。用户发消息后，系统会识别专业、学历、政治面貌、意向地区等明确字段，并写入当前会话记忆或长期用户画像。更新时会做基本的覆盖和去重，例如同一类会话记忆更新旧值，地区、部门等列表字段避免重复追加，side-query 返回的记忆名称也会去重。

所以总结一下，当前实现的流程是：先整理记忆目录，再由独立的 side-query 选择相关记忆，最后只把选中的内容注入主 Agent。记忆抽取和去重目前主要是规则化实现，还没有扩展成复杂的语义记忆系统。

### 项目实际

面试时不要讲太多函数名，可以直接说：当前项目已经实现了独立的 side-query 记忆选择链路，主 Agent 和政策回答都不会再直接加载全量长期记忆。自动抽取目前是规则化抽取，不是 LLM 抽取；去重主要是在写入、列表更新和记忆选择时做基础去重。真正的后台定期去重和语义合并还没有完整实现，后续会继续补。

## 11. 项目中的会话管理与上下文管理是怎么实现的？

### 建议回答

项目把会话管理和上下文管理分开，通过会话 ID 关联。用户发来消息后，系统先将会话和消息持久化到 PostgreSQL；调用 Agent 前，再根据当前问题读取最近消息、会话摘要、任务进度、用户偏好、长期画像、历史决策、附件摘要和检索结果，按需裁剪后组装上下文，不会加载全部历史。

短期工作状态和重复查询结果优先从 Redis 读取，以降低数据库和检索压力；长期数据和完整会话记录仍以 PostgreSQL 为准。Redis 未命中或不可用时，系统回源 PostgreSQL，因此缓存失效不会导致会话丢失。对话或工具结果过长时，系统会压缩旧内容；超过上下文预算后保存完整记录并生成连续性摘要，只将摘要和最近消息传给模型，以控制 Prompt 长度，同时保留审计和回放依据。

会话与登录用户的 `user_id` 绑定，用户再次登录后只能查询并恢复自己的会话和上下文。Redis 不保存完整会话历史，短期工作记忆和压缩摘要副本默认 TTL 为 24 小时，过期后可从 PostgreSQL 重新加载；完整会话、消息和长期记忆不受该 TTL 影响。

删除会话时，PostgreSQL 中的会话、消息、附件、会话记忆和关联缓存记录会删除，已登记的 Redis 查询缓存也会清理。由于短期工作记忆和压缩副本当前没有统一的会话级删除索引，它们不会被逐个扫描，而是依靠 TTL 自然过期。因此，Redis 负责短期加速，PostgreSQL 负责完整记录、恢复和审计。

所以可以概括为：先持久化会话，再按任务组装上下文，运行中用摘要和裁剪控制长度，Redis 负责加速短期状态，PostgreSQL 负责完整记录和长期恢复。

### 项目实际

当前实现中，会话、消息、附件、引用和 trace 都会持久化到 PostgreSQL；每次请求从当前会话和记忆数据中取出相关内容，再交给 Agent 运行。短期工作记忆、压缩摘要副本以及 RAG 和岗位查询结果使用 Redis 加速，并在需要时回源 PostgreSQL。Redis 主要承担短期状态和缓存职责，不负责保存完整会话历史。

## 12. 项目中的异常处理机制是怎么实现的？

### 建议回答

我会先对异常分类，再决定是重试、恢复、降级还是直接结束，而不是所有异常都盲目重试。异常处理主要集中在 Agent 调用链上，并通过 trace 记录恢复过程。

模型调用出现 429、529、超时、连接错误或临时服务错误时，系统会进行有限次数的重试，使用指数退避和随机抖动；如果连续多次遇到模型过载，并且配置了备用模型，就切换到备用模型继续执行。参数错误、认证错误等不可恢复异常不会反复重试，而是记录错误并结束当前调用。

如果模型输出因为 Token 限制被截断，系统第一次会提高输出预算；仍然截断时，会加入续写提示，让模型从中断位置继续生成，并限制续写次数。遇到上下文过长时，系统会触发一次更激进的上下文压缩，保存原始记录、生成摘要后再重试模型调用。

工具执行失败时，系统会把错误结果写回当前 Agent 上下文，让模型决定是否调整参数或换一条路径；外部服务则根据场景采用备用检索源、浏览器回填、数据库回源或确定性结果降级。这样局部服务失败不会直接导致整个会话崩溃。

所以整体上是：先分类异常，再按错误类型进行重试、压缩、续写、切换模型或降级；超过恢复上限后停止，并保留错误信息和 trace，方便定位和回放。

### 项目实际

当前项目已经在 Agent 主循环中接入了瞬时模型错误重试、指数退避、连续过载切换备用模型、输出截断扩容与续写、上下文超限响应式压缩，以及不可恢复错误的 trace 记录。每次恢复都会生成 `ErrorRecovery` hook，记录错误类型、重试次数、等待时间、压缩动作、模型切换和最终状态。

除此之外，项目还实现了网页检索改写重试和浏览器回填、岗位研究证据不足时的有限重试、Redis 失败后的 PostgreSQL 回源、记忆 side-query 超时后的安全跳过，以及流式回答失败后的非流式生成降级。数据库启动阶段也有连接重试。

当前这些恢复机制主要覆盖 Agent 主循环和各个外部服务边界，还没有把所有异常统一抽象成一个全局状态机；完整的离线故障注入、重试成本监控和统一恢复看板仍可以继续补充。

## 面试时的统一表达

1. 先讲整体思路，再补具体实现。
2. 岗位硬筛依赖 PostgreSQL，RAG 只负责政策和风险证据。
3. 当前是主 Agent 加专项模块，不要夸大成完全自主、完全并行的多 Agent。
4. 没有真实实验数据时，不报具体提升百分比。
5. 没有训练过的模型组件，不说成自己训练或实现过。
