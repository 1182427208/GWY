# 启动文档

本文档用于本地开发环境启动，和 [README.md](/e:/GwyPilot/GwyPilot/README.md) 保持一致。

## 环境要求

- Python `3.10`
- Node.js `20+`
- Bun `1.x`
- Docker Desktop / Docker Compose

## 启动顺序

推荐顺序：

1. 启动 Milvus
2. 启动 PostgreSQL / Redis
3. 启动后端
4. 启动前端

## 1. 启动 Milvus

首次启动，或者需要重建 Milvus 时，在 PowerShell 中执行：

```powershell
cd "E:\GwyPilot\GwyPilot\docker\milvus"
docker compose up -d
```

查看日志：

```powershell
docker compose logs -f
```

说明：

- `docker/milvus/docker-compose.yml` 会启动 Milvus 及其依赖服务
- `docker/milvus/volumes/` 是本地运行数据目录，不需要上传到 GitHub

## 2. 启动 PostgreSQL 和 Redis

如果你已经有可用的本地 PostgreSQL / Redis，可以直接复用。
如果你使用模板默认方式，可以在项目根目录通过 Docker Compose 启动依赖服务。启动前请先确认 `.env` 中这些变量已经正确配置：

- `POSTGRES_SERVER`
- `POSTGRES_PORT`
- `POSTGRES_DB`
- `POSTGRES_USER`
- `POSTGRES_PASSWORD`
- `REDIS_URL`
- `MILVUS_URI`

## 3. 启动后端

在新的 PowerShell 窗口中执行：

```powershell
cd "E:\GwyPilot\GwyPilot\backend"
conda activate CareerFlow
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir app --no-access-log
```

如果你平时使用 Conda 或虚拟环境，先激活对应环境，再执行上面的命令。

## 4. 启动前端

在新的 PowerShell 窗口中执行：

```powershell
cd "E:\GwyPilot\GwyPilot\frontend"
npm run dev -- --host 0.0.0.0 --port 5173
```

说明：

- 根目录的 `npm run dev` 会转发到 `frontend`
- 这是当前项目推荐的前端启动方式

## 访问地址

- 前端：`http://localhost:5173`
- 后端 API：`http://localhost:8000`
- Swagger：`http://localhost:8000/docs`
- Milvus：`http://localhost:19530`

如果你启用了模板自带的完整 Docker 服务，还可能会用到：

- Adminer：`http://localhost:8080`
- Traefik 面板：`http://localhost:8090`

## 常用命令

后端测试：

```bash
cd backend
bash ./scripts/test.sh
```

后端 lint：

```bash
cd backend
bash ./scripts/lint.sh
```

后端格式化：

```bash
cd backend
bash ./scripts/format.sh
```

前端 lint：

```bash
bun run --filter frontend lint
```

前端测试：

```bash
bun run --filter frontend test
```

## 说明

- `giftia_ref/` 只是参考目录，不属于项目正式代码
- `docker/milvus/volumes/`、`.playwright-cli/`、`data/gwy_analysis_reports/` 这类目录属于本地运行产物，已经加入忽略规则
- 如果前端或后端端口被占用，优先检查本地是否有旧进程没有关闭
